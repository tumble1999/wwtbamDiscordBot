const Discord = require('discord.js');
const client = new Discord.Client();

module.exports {
  win:function functionName(channel) {
    
  }
}


const token = process.env.DISCORD_SFX_TOKEN || require('./config/token.js').sfxtoken;
discordbot.login(token);
